name: Playwright Tests
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  test:
    timeout-minutes: 15
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # --------------------
      # Node / Frontend setup
      # --------------------
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install NPM dependencies
        run: npm ci

      # --------------------
      # PHP / Laravel setup
      # --------------------
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.5
          extensions: mbstring, pdo, pdo_mysql
          coverage: none

      - name: Install Composer dependencies
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Prepare Laravel environment
        run: |
          cp .env.e2e.example .env
          touch database/database.sqlite
          chmod 777 database/database.sqlite
          php artisan key:generate
          php artisan key:generate --show
          php artisan migrate:fresh --seeder=E2eDatabaseSeeder

      - name: show env
        run: cat .env

      - name: Build frontend
        run: npm run build

      # --------------------
      # Start Laravel server
      # --------------------
      - name: Start Laravel server
        run: php artisan serve --host=0.0.0.0 --port=8000 > laravel.log 2>&1 &

      - name: Wait for Laravel TCP port
        run: npx wait-on tcp:127.0.0.1:8000 --timeout 10000

      - name: Test Laravel endpoint
        run: curl -i http://127.0.0.1:8000/testing/login

      # --------------------
      # Playwright
      # --------------------
      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npx playwright test

      # --------------------
      # Artifacts (optional)
      # --------------------
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report

      - name: Dump Laravel logs
        if: failure()
        run: tail -n 300 laravel.log || true
